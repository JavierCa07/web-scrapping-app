import { Box } from "@mui/material";
import InfoIcon from '@mui/icons-material/Info';

export const AppDescription = () => (
    <Box sx={{ width: '600px', marginBottom: '100px' }}>
        <InfoIcon />
        This program allows the student to be able to find the original source of where a quote is from to help them cite their essays.
    </Box>
)